[project]
name = "multimodal-ai"
version = "0.1.0"
description = "Visual Question Answering with Gemini & LangChain"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "python-multipart>=0.0.9",
    "langchain-google-genai>=1.0.0",
    "python-dotenv>=1.0.0",
    "pillow>=11.3.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.1.0",
    "ipykernel>=6.0.0",
]

[tool.pytest.ini_options]
pythonpath = "."
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["notebooks/*.ipynb"]
